## Driver Adapters

An **adapter** is a software library or middleware that connects an **application** to a **database**.

- `pg` → Node.js PostgreSQL driver
- `@prisma/adapter-pg` → connects Prisma Client to `pg`
