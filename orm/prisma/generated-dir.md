## Schema Generated

Reads `schema.prisma` and builds TypeScript code that can be imported. It generates the **Application Code Types**.

- `generate` → changes the TypeScript code
#### Create `generated` Directory

```shell
npx prisma generate
```


